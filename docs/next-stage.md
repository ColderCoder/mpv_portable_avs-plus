# Next stage

The project has completed the build framework stage.

Next engineering milestone:

1. Import a verified FFmpeg AVS+ patch series.
2. Build FFmpeg with AVS1-P16 support.
3. Link mpv against the patched FFmpeg.
4. Run playback regression with AVS+ HDTV TS files.

A successful build must decode AVS+ video, not only identify `cavs` streams.
