# AVS+ patch area

Place the FFmpeg AVS+ patch series here.

Target:

- FFmpeg libavcodec cavs decoder
- AVS1-P16 / AVS+ HDTV streams
- libcavs integration

The build script will apply patches from this directory before compiling FFmpeg.
