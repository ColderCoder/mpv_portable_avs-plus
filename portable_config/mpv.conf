# AVS+ HDTV portable mpv configuration

# AVS+ requires libcavs-enabled FFmpeg build
hwdec=no

# MPEG-TS broadcast recordings
demuxer-lavf-format=mpegts

# Better sync for 25fps HDTV captures
video-sync=display-resample

# AC-3 5.1 passthrough (enable if receiver supports it)
# audio-spdif=ac3
